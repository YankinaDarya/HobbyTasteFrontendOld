import React, {Component} from 'react';
import s from './Footer.module.css';


const Footer = (props) => {
    return (<footer className={s.footer}>HobbyTaste Footer</footer>);
};

export default Footer;