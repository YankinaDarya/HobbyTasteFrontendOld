import React, {Component} from 'react';
import s from './Header.module.css';


const Header = (props) => {
    return (<header className={s.header}>HobbyTaste Header</header>);
};

export default Header;